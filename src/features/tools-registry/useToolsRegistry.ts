import { useTools } from './ToolsProvider';

/**
 * Hook for accessing the Tools Registry context
 * Re-exports useTools for convenience and consistency with naming conventions
 */
export const useToolsRegistry = useTools;
