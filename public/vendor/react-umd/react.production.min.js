// React 19.1.0 UMD Production Build Placeholder
// In production, this should be replaced with the actual React 19.1.0 UMD build
// from https://unpkg.com/react@19.1.0/umd/react.production.min.js
(function(global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
  typeof define === 'function' && define.amd ? define(['exports'], factory) :
  (global = global || self, factory(global.React = {}));
}(this, (function(exports) {
  'use strict';
  
  // Minimal React placeholder for development
  // This is a placeholder - replace with actual React UMD build
  const React = {
    createElement: function(type, props, ...children) {
      return { type, props: { ...props, children: children.length === 1 ? children[0] : children } };
    },
    Fragment: Symbol.for('react.fragment'),
    version: '19.1.0'
  };
  
  Object.assign(exports, React);
  exports.default = React;
})));
